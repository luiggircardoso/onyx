<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="onyx_app.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:onyx_app"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="onyx-app">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <!-- The main container for the entire page. -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Spacing="10" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="20">
            <!-- Title -->
            <TextBlock Text="Onyx" FontSize="24" FontWeight="Bold" HorizontalAlignment="Left" Margin="0,0,0,10" />

            <!-- Link Input Section -->
            <TextBlock Text="Video Link:" />
            <TextBox x:Name="URLinput" PlaceholderText="Youtube link goes here..." />

            <!-- File Path Section -->
            <TextBlock Text="Output File Path:" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="locationPath" Grid.Column="0" PlaceholderText="Select a save location..." />
                <Button Grid.Column="1" Content="Browse" Margin="5,0,0,0" Click="SetFolderPath" />
            </Grid>

            <!-- Download Button -->
            <Button x:Name="DownloadButton" Click="DownloadVideo" Content="Download" HorizontalAlignment="Stretch" Margin="0,20,0,0"/>
            <ProgressBar x:Name="DownloadProgressBar" Value="0" Width="Auto" Visibility="Collapsed" ShowPaused="False" ShowError="False" />
        </StackPanel>
    </Grid>
</Window>
